# joeblew999/.github — agent guide

The single source of truth for this repo — usage, authoring conventions, the
by-reference flows, and the release process — is the root
**[README.md](./README.md)**.

Agents (Claude Code, Cursor, Copilot, …): read README.md before touching mise tasks,
CI, releases, or skills, and work *with* the flows it describes — never reinvent
them. Every consumer repo should carry a `CLAUDE.md` that points here first.
