#!/usr/bin/env nu
#MISE description="dry-run of secrets:sync-github (show plan, change nothing)"

# Delegate to ./sync-github --dry-run.
let here = (path self | path dirname)
^$"($here)/sync-github" --dry-run
