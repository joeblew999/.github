#!/usr/bin/env bash
#MISE description="list secrets set on a deployed Worker (values hidden)"
#USAGE flag "--env <env>" help="Cloudflare environment" env="ENV" default="production"
set -euo pipefail

ENV="${usage_env:-${ENV:-production}}"
exec wrangler secret list --env "$ENV" "$@"
