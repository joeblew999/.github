#!/usr/bin/env bash
#MISE description="list secrets set on a deployed Worker (values hidden) — ENV=staging mise run wrangler:secret-list"
set -euo pipefail

ENV="${ENV:-production}"
exec wrangler secret list --env "$ENV" "$@"
