[task_config]
includes = ["mise-tasks"]

[tools]
"github:jdx/fnox"        = "1.24"
"github:nushell/nushell" = "0.112"   # pinned — bw:* shared tasks are nu (cross-platform; bash unavailable on Windows)
"aqua:cli/cli"           = "2"
"aqua:jqlang/jq"         = "1"
"cargo:usage-cli"        = "3"
# utm-dev Rust CLI — add once utm-dev is rewritten as a Rust binary:
# "cargo:utm-dev" = "3"
