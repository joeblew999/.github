#!/usr/bin/env bash
#MISE description="dry-run of secrets:sync-github (show plan, change nothing)"
set -euo pipefail

# Delegate to sync-github with --dry-run flag.
# Find sibling task script regardless of whether running from cache or local clone.
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$HERE/sync-github" --dry-run
