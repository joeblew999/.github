#!/usr/bin/env bash
#MISE description="tauri ios init — generate the Xcode project (macOS only, run once per clone)"
set -euo pipefail
if [[ "$(uname -s)" != "Darwin" ]]; then
  echo "✗ iOS builds require macOS" >&2; exit 1
fi
${TAURI_CMD:-tauri} ios init "$@"
